import LoginComponent from "@/components/auth/login";

const LoginPage : React.FC = () => {
    return (
        <div>
            <LoginComponent />
        </div>
    );
}

export default LoginPage;