/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "legendsofwindemere.com",
            "i.imgur.com",
            "i.pinimg.com",
        ],
    },
};

module.exports = nextConfig;
